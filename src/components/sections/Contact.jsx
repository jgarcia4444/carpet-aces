import React from 'react';

import SectionTitle from '../../shared/text/SectionTitle';

const Contact = () => {

    return (
        <div id="contact" className="mt-24 px-2 flex flex-col">
            <SectionTitle title="Contact"/>
        </div>
    )
};

export default Contact;